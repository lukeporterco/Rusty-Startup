version: 1
active_slice: "SLICE-002-package-root-load-folder-and-layout-resolution"
stage: "stage-1"
working_directory: "managed"
notes:
  - "This slice now provides a stable bootstrap-local self-package layout resolver result for rustystartup.core."
  - "Resolver outputs include runtime basis/source, package root and detection source, selected active content root, layout decision basis, load-folder gate status, managed assembly path, native payload path, and structured failure reason."
  - "Resolver logic derives roots from discoverable package layout, runtime version basis, managed self-location hint, and explicit self-package LoadFolders.xml evidence."
  - "Blind mod-root-relative 1.6/Native path assumptions are removed; managed assembly and native payload paths derive from the same selected active content root."
  - "Duplicate or nested rustystartup.core identities fail explicitly."
  - "Unsupported or ambiguous LoadFolders.xml rules (including conditional gates the bootstrap-local resolver cannot prove) fail explicitly."
  - "Keep package identity locked to rustystartup.core."
  - "Because no test or fixture surface is currently inside SLICE-002 allowed paths, this slice can be implementation-complete but cannot claim evidence-complete closure yet."
  - "Do not move Rust engine semantics, snapshot/replay/equivalence logic, or XML discovery/patch ownership into this slice."
