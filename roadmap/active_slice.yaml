version: 1
active_slice: "SLICE-002-package-root-load-folder-and-layout-resolution"
stage: "stage-1"
working_directory: "managed"
notes:
  - "This slice establishes self-package root detection, active load-folder selection, and package-relative layout resolution."
  - "Resolver logic must derive roots from package layout and explicit load-folder evidence, not from hardcoded machine paths."
  - "Keep package identity locked to rustystartup.core."
  - "Do not move Rust engine semantics, snapshot/replay/equivalence logic, or XML discovery/patch ownership into this slice."
