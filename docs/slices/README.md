# Slice Specs

This directory contains per-slice specifications and the drafting template for new slices.

The canonical stage and slice registry lives in `roadmap/roadmap.yaml`.

New slice specifications in this directory must use slice IDs and titles already declared in `roadmap/roadmap.yaml`.

Use `docs/slices/_template.md` as the starting point for manual first drafts.